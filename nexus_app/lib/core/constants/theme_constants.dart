import 'package:flutter/material.dart';

abstract final class ThemeConstants {
  // ── Mirror: dark velvet-purple ────────────────────────────────────────────────
  static const Color mirrorBg = Color(0xFF0A0A12);
  static const Color mirrorPrimary = Color(0xFF7B2FBE);
  static const Color mirrorSecondary = Color(0xFF9D4EDD);
  static const Color mirrorAccent = Color(0xFFBB86FC);
  static const Color mirrorSurface = Color(0xFF1A1A2E);
  static const Color mirrorGlow = Color(0x887B2FBE);

  // ── Fracture: blood red ───────────────────────────────────────────────────────
  static const Color fractureBg = Color(0xFF0D0000);
  static const Color fracturePrimary = Color(0xFFD32F2F);
  static const Color fractureSecondary = Color(0xFFFF1744);
  static const Color fractureAccent = Color(0xFFFF5252);
  static const Color fractureSurface = Color(0xFF1A0000);
  static const Color fractureGlow = Color(0x88D32F2F);

  // ── Arena: near-black with gold ───────────────────────────────────────────────
  static const Color arenaBg = Color(0xFF090909);
  static const Color arenaPrimary = Color(0xFFFFD700);
  static const Color arenaSecondary = Color(0xFFFFA000);
  static const Color arenaAccent = Color(0xFFFFEB3B);
  static const Color arenaSurface = Color(0xFF151510);
  static const Color arenaGlow = Color(0x88FFD700);

  // ── Persona identity colours ──────────────────────────────────────────────────
  static const Color shadowNexusColor = Color(0xFFFF1744);
  static const Color primeNexusColor = Color(0xFF00B0FF);

  // ── Typography ────────────────────────────────────────────────────────────────
  static const Color textPrimary = Color(0xFFE0E0E0);
  static const Color textSecondary = Color(0xFF9E9E9E);
  static const Color textDisabled = Color(0xFF616161);

  // ── Semantic ──────────────────────────────────────────────────────────────────
  static const Color successColor = Color(0xFF00E676);
  static const Color warningColor = Color(0xFFFFAB00);
}
