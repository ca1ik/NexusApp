import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — app bootstraps', () {
    // Full widget tests require Firebase mock setup.
    // See test/features/ for BLoC and Cubit unit tests.
    expect(1 + 1, 2);
  });
}
